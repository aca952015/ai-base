# 外部系统连接规范

外部 SaaS 凭证只保存在 OpenConnector 中。Agent Runtime 通过 HTTP Action API 调用，并负责业务身份、审批、幂等和审计。

- 默认禁用通用 provider proxy。
- 写操作携带唯一 `Idempotency-Key`。
- Token、Prompt 和 Trace 中不得出现 OAuth access/refresh token。
- 默认每个组织使用独立 OpenConnector 实例、加密键和数据卷。
